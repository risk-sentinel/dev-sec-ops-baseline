# Python bytecode. Three .pyc files reached v0.6.0 by way of `git add -A tools/`
# with no ignore rule in place, and vendoring copied them into every consumer.
__pycache__/
*.py[cod]

# InSpec vendoring output — resolved dependencies, never source.
vendor/
